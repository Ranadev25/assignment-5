
    - 1️⃣ What is the difference between var, let, and const?
     Ans: 
     1.var = var is function-scoped and can be redeclared and reassigned.
     2.let = let is block-scoped, can be reassigned but not redeclared.
     3.const = const is block-scoped and cannot be reassigned or redeclared.

    - 2️⃣ What is the spread operator (...)?
      Ans:
      * The spread operator is used to expand elements of an array or object into another array or object.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
       Ans:
       1.map = map() creates and returns a new array after transforming elements.
       2.filter = filter() returns a new array with elements that match a condition.
       3.forEach = forEach() loops through the array but does not return a new array.

    - 4️⃣ What is an arrow function?
       Ans:
       * An arrow function is a short syntax for writing functions in JavaScript using =>.

    - 5️⃣ What are template literals?
       Ans:
       * Template literals are strings written with backticks ( ) that allow embedded variables using ${}.



- **For login input**

Username: admin

Password: admin123



## 📤  Submit link

- **Assignment-5 GitHub Repository Link:**
   https://github.com/Ranadev25/assignment-5

- **Assignment-5 Live Site Link:**
   https://issuestracke.netlify.app/


- **Linkedin Live Site Link:**
    https://www.linkedin.com/in/rana-khan-dev
---


